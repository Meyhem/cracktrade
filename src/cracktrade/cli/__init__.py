"""Command-line interface.

This package parses arguments, calls the core library, and renders the typed results it gets
back. It contains no engine logic -- anything worth doing lives in the library so the future
API interface gets it for free.
"""

from __future__ import annotations
